# Cut from this book — port to the friction book

`04-the-frictional-method-...md` was cut from *Computational Skepticism for AI* (2026-07-01).

**Why:** the reframed book is about *validating what you build with AI* — datasets,
pipelines, agents, charts, claims. The frictional method is about *assessment of human
learning* ("when AI can generate the artifact, what is evidence that the student learned?").
Different subject, different reader — and it already has a home: **`friction-measuring-the-learning-struggle`**.
It was also the manuscript's one **Weak** chapter (the GLP / Y-component / neurobiology
apparatus asserted without sources — the book's own fluency-trap sin). Its one transferable
kernel — *"a working artifact is not evidence that the judgment happened"* — is already the
book's spine (fluency trap + solve–verify asymmetry), so it needs no chapter here.

**Action:** port this file into `friction-measuring-the-learning-struggle`, where it can be
as deep as it wants. Do not recompile it into this book.
