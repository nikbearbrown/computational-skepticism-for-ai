# Media Plan — Chapter 5: Model Explainability: Distinguishing Explanation from the Appearance of Explanation

Zones detected: 14 · video 3 · figure 0 · graph 3 · table 0 · served 6 · dropped 2

Scope note: end-of-chapter apparatus (Glimmer, Exercises, LLM Exercise) not scanned. The chapter already carries 8 CAJAL figures and ~7 styled tables; only unserved zones are carded below.

## Zone index
| # | Concept (one line) | Heuristic | Routed | Priority/Score |
|---|---|---|---|---|
| 1 | Fluency trap: a technically accurate explanation increases confidence in the wrong direction (radiologist + shortcut learning + zip-code attribution) | MC | video | 9/10 (Candidate 01) |
| 2 | Shapley mechanism: marginal contribution averaged over all arrival orders ("features entering a room") | MC | video | 8/10 (Candidate 04) |
| 3 | Ash "deleted": same word, two language games; the audience question | MC | video | 8/10 (Candidate 05) |
| 4 | LIME explanations vary run-to-run on the identical input | PQ | graph | Important |
| 5 | Exact Shapley cost grows as 2^\|F\| — 10 features = 1,024 subsets, 30 = over a billion | PQ | graph | Important |
| 6 | Marginal sampling of correlated features produces off-manifold "Frankenstein instances" | PQ/VG | graph | Important |
| 7 | What SHAP can / cannot answer, by Pearl rung | CT | SERVED | — |
| 8 | Four Shapley axioms: guarantees, non-guarantees, failure modes | EN | SERVED | — |
| 9 | Worked 3-feature Shapley example and Efficiency check | MC | SERVED | — |
| 10 | SHAP approximation methods (Exact / KernelSHAP / Permutation / TreeSHAP) | SP | SERVED | — |
| 11 | Transparency vs explainability vs interpretability | CT | SERVED | — |
| 12 | Three explanation families (SHAP / LIME / counterfactual) by rung and failure mode | CT | SERVED | — |
| 13 | KernelSHAP variance vs sample count M; antithetic sampling benefit | PQ | dropped | passing detail, no argumentative weight |
| 14 | LIME locally-linear vs globally-nonlinear neighborhood mismatch | VG | dropped | partially served by Fig 5.6; weak standalone |

## Graph cards

```
GRAPH 1 — LIME explanations are unstable across repeated runs on the same input
Priority: Important
Reader question: How much do LIME's top-feature coefficients spread when the identical input is explained 50 times?
Family + first candidate: distribution → strip/box plot (one box per feature, coefficient across runs)
Data status: spec needed (no dataset in the book; trivially generated by scripting LIME on any public model)
DATA SPEC:
  Unit of observation: one LIME run × one feature
  Fields: run_id : int, feature : categorical, coefficient : float, rank : int
  Denominator: counts (raw coefficients)
  Expected n: ~50 runs × top 8 features = 400 rows
  Likely source: script running LIME (default settings) repeatedly on one input of a public tabular or text classifier
Exclusions: no SHAP comparison series; no perturbation-count sensitivity sweep — one default configuration only; no averaged/"stabilized" LIME variants
```

```
GRAPH 2 — The exponential blow-up of exact Shapley computation
Priority: Important
Reader question: How fast does the number of coalitions 2^|F| outgrow anything computable, and what does sampling buy back?
Family + first candidate: relationship → line chart, log-scale y: 2^F vs F for F = 1..40, with a linear sampled-evaluations line (fixed M) overlaid
Data status: provided (deterministic formula; no empirical data required)
DATA SPEC:
  Unit of observation: one feature count F
  Fields: F : int, exact_subsets : float (2^F), sampled_evals : float (M·F, M fixed e.g. 1000)
  Denominator: counts
  Expected n: 40 rows
  Likely source: computed directly
Exclusions: no TreeSHAP O(TLD) curve (parameters are model-specific — caption note only); no wall-clock benchmarks (would require measurement the book does not supply)
```

```
GRAPH 3 — Off-manifold "Frankenstein instances" from marginal sampling
Priority: Important
Reader question: Where do marginal-sampling coalition points land relative to the real joint distribution of two correlated features?
Family + first candidate: relationship → scatter (real data cloud vs marginal-sampled points, two colors)
Data status: illustrative-only possible — synthetic by design; the book presents the mechanism, not a dataset. Must be labeled "synthetic demonstration," never as findings.
DATA SPEC:
  Unit of observation: one instance
  Fields: x3 : float, x4 : float, origin : categorical (real | marginal-sample)
  Denominator: counts
  Expected n: ~500 + 500 points
  Likely source: synthetic bivariate normal (ρ ≈ 0.9) plus an independent-marginal resample of the same margins
Exclusions: no third conditional-sampling series (one contrast only — the marginal/conditional CHOICE is already served by Fig 5.5); no model-output coloring; no Aas et al. estimator detail
```

## Table cards

None. The chapter's existing tables (SHAP can/can't, axioms, methods, term triad, three families, worked-example orderings) cover every lookup/contrast zone. No new table is justified.

## Served zones
- SERVED — What SHAP can/cannot answer by Pearl rung: 5-row comparison table after "What SHAP is, and what SHAP isn't."
- SERVED — Four Shapley axioms (guarantees / non-guarantees / failure modes): axioms table plus the explicit Additivity/Linearity naming reconciliation in prose.
- SERVED — Worked Shapley example and Efficiency check: orderings table + Fig 5.4 force plot.
- SERVED — SHAP approximation method lookup: methods comparison table (complexity, exactness, dependency handling, use case).
- SERVED — Marginal vs conditional SHAP as a meaning choice: Fig 5.5.
- SERVED — Transparency / explainability / interpretability triad, and three-explanation-families contrast: two comparison tables; Ash two-reports contrast: Fig 5.8; language-game mismatch: Fig 5.7.
- SERVED-POORLY — SHAP-vs-LIME contrast (Fig 5.6): the image alt text and caption are truncated to literally "SHAP vs" — the figure cannot be verified to teach the contrast it is placed for. Suggest: repair alt/caption first; if the underlying figure is equally underspecified, replace with a small two-panel comparison figure (SHAP = game-theoretic accounting of the whole prediction; LIME = local linear surrogate in a neighborhood).

## Pointers
- Figure cards: none — no pantry/05-…-cajal.md written (no new figures justified; chapter carries 8 already).
- Video candidates from this chapter: vids/video-ideas.md — Candidates 01, 04, 05 in this scouting pass (parent assembles and renumbers).
